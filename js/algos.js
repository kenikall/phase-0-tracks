//take in array
//initialize a baseline length of 0
//loop through the array starting at the beginging
//find the lenght of current element
//if the length of the current elemement is longer than the baseline make the current element the baseline 
//after looping through the array return baseline

longest_word(["long phrase","longest phrase","longer phrase"]);